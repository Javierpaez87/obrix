{
  "name": "obrix-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "postinstall": "cd packages/config && npm install && cd ../ui && npm install && cd ../../apps/app && npm install && cd ../admin && npm install",
    "dev": "concurrently \"npm run dev:app\" \"npm run dev:admin\"",
    "dev:app": "cd apps/app && npm run dev",
    "dev:admin": "cd apps/admin && npm run dev",
    "build": "npm run build:packages && npm run build:apps",
    "build:packages": "cd packages/config && npm run build && cd ../ui && npm run build",
    "build:apps": "cd apps/app && npm run build && cd ../admin && npm run build",
    "build:app": "npm run build:packages && cd apps/app && npm run build",
    "build:admin": "npm run build:packages && cd apps/admin && npm run build",
    "lint": "cd apps/app && npm run lint && cd ../admin && npm run lint",
    "seed": "tsx scripts/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "concurrently": "^8.2.2",
    "prettier": "^3.2.4",
    "tsx": "^4.7.0"
  }
}
